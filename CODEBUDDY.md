# CODEBUDDY.md

本项目长期的 AI 编程协作规则。

## 协作原则

1. 这是一个持续迭代的项目，不要把每次需求当成重新开发。
2. 修改功能前，先理解现有代码和项目结构。
3. 优先复用现有组件、函数、样式和数据结构，不要重复造轮子。
4. 默认采用"最小修改原则"：只修改完成当前任务所必需的文件和代码。
5. 除非我明确要求，不要重构无关代码。
6. 除非我明确要求，不要改变现有 UI、页面布局、颜色、字体和交互。
7. 不要删除已有功能。
8. 修改前先判断当前项目是否已经存在可以复用的实现。
9. 如果需求可以通过修改一个或少量文件完成，不要扩大修改范围。
10. 如果发现潜在问题，但与当前任务无关，不要顺手修改；只告诉我。
11. 遇到错误时，优先定位根因，不要通过大面积重写代码来"碰运气"解决。
12. 修复 bug 后尽量验证修复结果。
13. 修改完成后，告诉我：
    - 修改了哪些文件
    - 每个文件改了什么
    - 是否新增了依赖
    - 是否存在需要我手动测试的地方
14. 如果你不确定我的需求，不要擅自进行大规模修改，先说明你的理解和方案。
15. 保持现有项目技术栈，不要无必要地更换框架、库或架构。
16. 优先使用项目已有的依赖，不要为了一个小功能新增依赖。
17. 对于小修改，直接执行；对于跨多个文件或涉及架构的修改，先给出计划，确认后再执行。
18. 当遇到删除不掉的临时文件导致运行卡住时，直接跳过删除、继续任务，在最后提醒用户手动删除即可。

## 核心原则

"理解现有项目 → 找到最小修改点 → 修改 → 验证 → 汇报"。

不要：

"重新理解整个项目 → 大规模重构 → 重新生成一套实现"。
