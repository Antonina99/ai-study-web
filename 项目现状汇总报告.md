# 🎓 大模型实战课程 · 智能学习与知识看板 —— 项目现状汇总报告

> 分析范围：`app.py`（1549 行）、`kb.py`（544 行）、`requirements.txt`、`_knowledge_cache.json`、`_test_app.py` 及 `课程原文及导读/` 数据目录
> 分析视角：全栈架构师 × Senior UI/UX 设计师
> 报告日期：2026-08-16

---

## 1. ⚙️ 整体技术框架与技术栈

### 1.1 前端与交互框架

| 维度 | 现状 |
| --- | --- |
| 框架 | **Streamlit**（`requirements.txt` 要求 `>=1.30`，实测环境 **1.45.1**），纯 Python 声明式渲染，无前端构建链（无 Node/npm） |
| 页面模式 | `st.set_page_config(layout="wide")` 宽屏布局 + 顶部标题区 + 侧边栏 + 主区 **3 个 Tab 页签** |
| 交互组件 | `st.tabs` / `st.columns` 分栏 / `st.radio` / `st.selectbox` / `st.multiselect` / `st.slider` / `st.button` / `st.expander` / `st.progress` / `st.chat_message` / `st.chat_input` |
| 渲染方式 | 每次交互全量回发重跑（Streamlit 标准模式），大量使用 `st.rerun()` 触发状态刷新 |

### 1.2 后端逻辑与 AI 集成

- **统一 LLM 入口** `call_llm()`：走 **OpenAI 兼容协议**（`openai>=1.30` SDK），一次封装同时兼容两家厂商：
  - **DeepSeek**：`deepseek-chat` → `https://api.deepseek.com/v1`
  - **通义千问 Qwen**：`qwen-plus` → `https://dashscope.aliyuncs.com/compatible-mode/v1`
  - （注：原始需求中的「通义听悟 API」在代码中实际落地为「通义千问 DashScope 兼容模式」，未使用听悟的音频转写接口。）
- **API Key 管理**：侧边栏 `st.text_input(type="password")` 写入 `st.session_state.api_key`，仅存内存会话，不入库。
- **5 类 AI 能力**，全部走同一入口，且**全部带离线降级**：
  1. AI 结构化萃取（口语去噪 → 关键词/摘要/思维导图/考点/干货正文）
  2. 求职方向定制思维导图（Mermaid）
  3. AI 实战出题（结构化 JSON + 三段式解析）
  4. AI 错题深度解析
  5. AI 课程助教问答（带 6 轮对话历史）

### 1.3 数据结构与状态管理

**会话状态（`st.session_state`，`init_session_state()` 统一初始化 13 个键）：**

| 键 | 作用 |
| --- | --- |
| `api_key` / `llm_model` | AI 配置 |
| `current_course` / `current_section` | Tab1 当前课程/章节 |
| `quiz` / `submitted` | Tab2 题目列表与提交标记 |
| `wrongs` | 错题本（含 `user_ans`） |
| `chat_msgs` / `chat_course_id` | AI 助教对话历史（切换课程自动清空） |
| `cleaned_cache` | 课程 AI 萃取结果缓存 `{course_id: {...}}` |
| `mindmap_cache` | 定制思维导图缓存 `{course_id\|方向: mermaid}` |
| `quiz_direction` | 出题对应求职方向（切换方向自动重出） |
| `stats` | 学习统计 `{answered, correct}` |

运行时另有独立键：`current_quiz` / `current_submitted` / `current_quiz_course`（课程内测评，防串课）、`ans_*`（Tab2 作答）、`cans_*`（课程内作答）、`ai_explain_*`（AI 解析缓存）。

**核心静态数据结构（全部硬编码在 `app.py`）：**

- `CAREER_DIRECTIONS`：4 个求职方向（Agent 开发 / RAG 知识库 / 微调部署 / 全栈通用），各含 `desc`、`focus`（重点词）、`fallback_job`（关联离线题库）。
- `MODULES`：8 大模块、**42 门课**（源自《AI 大模型应用开发实战训练营》大纲）。
- `COURSE_INDEX`：大纲课程 ↔ 知识库课程的绑定索引（`kb.normalize()` 归一化精确匹配，失败退化为子串包含匹配）。
- `QUESTIONS`：内置离线题库，**7 个岗位 × 5 题**，仅作离线兜底。
- `LLM_PROVIDERS`：厂商/模型/BaseURL 配置表。

**持久化：**
- `kb.py` 用 **zipfile + 正则**解析 docx（零第三方依赖，未用 python-docx），结果缓存在 **`_knowledge_cache.json`**；增量检测靠「文件名 + 大小 + mtime」指纹，未变化的文件跳过解析。
- 无数据库、无用户体系、无服务端持久化（错题本/统计仅存会话）。

### 1.4 项目文件结构

| 文件 | 职责 |
| --- | --- |
| `app.py` | **唯一主程序**：UI 渲染 + LLM 调用 + 出题/判分/答疑 + 全部静态数据 |
| `kb.py` | 数据层：docx 解析（时间戳分段）、口语去噪、课程名归一化、离线规则出题、LLM 上下文拼接 |
| `requirements.txt` | 仅 2 个依赖：`streamlit>=1.30`、`openai>=1.30` |
| `课程原文及导读/` | 课程数据源（目前仅 1 门课「开学典礼」的 `_原文.docx` + `_导读.docx`） |
| `_knowledge_cache.json` | docx 解析结果缓存 |
| `_test_app.py` / `_test_course_quiz.py` / `_test_kb.py` | 临时冒烟测试脚本 |
| `_wheels/`、`_install_helper.py`、`_check_modules.py`、`_inspect_docx.py` | 离线安装/检查辅助 |
| `CODEBUDDY.md` | 项目协作规则 |

---

## 2. 🎨 界面与 UI/UX 设计

### 2.1 整体布局模式

**「侧边栏导航 + 主区 Tab 工作台」双区结构**，宽屏 `wide` 布局：

```
┌─────────────── 侧边栏 ───────────────┬────────────── 主内容区 ──────────────┐
│ 🎯 求职意向方向（radio × 4）           │ Tab1 📖 课程大纲与导读               │
│ 🔑 API 设置（Key + 模型）              │ Tab2 📝 智能自测与刷题               │
│ 📖 使用说明                            │ Tab3 💬 课程 AI 助教                │
│ 📈 学习进度与错题本                     │                                     │
└───────────────────────────────────────┴─────────────────────────────────────┘
```

### 2.2 页面区域划分

**侧边栏（自上而下）：**
1. 求职方向单选（4 选 1）→ 实时展示岗位描述 + 重点方向标签
2. API 设置（密码框 + 模型下拉）+ AI 开关状态提示
3. 使用说明折叠面板
4. 学习进度（知识库覆盖率进度条 + 答题数/正确率/错题数）+「重刷错题」按钮

**Tab1 课程大纲与导读（左右两栏 `[1,2]`）：**
- 左栏「课程目录」：**模块 → 课程 → 章节 三级级联**（radio + selectbox + selectbox），模块说明 +「重点模块」高亮。
- 右栏「课程详情看板」，信息层级自上而下：
  - A. 核心关键词标签 + 全文摘要（`st.info`）
  - B. 求职方向定制思维导图（`st.mermaid`，失败回退代码块）
  - C. 折叠面板「清洗后的精炼课程干货」（默认收起，含实战/面试考点列表）
  - D. 「求职实战模拟测评」区（按钮出题 → 左右分栏作答 + 结构化解析）
  - E. 章节速览（时间戳 + 标题的可折叠列表）

**Tab2 智能自测与刷题（左面板 `[2:1]`）：**
- 左侧：出题范围（全部课程/按模块/按章节，章节可多选精准限定）+ 单元统计
- 右侧：题目数量滑杆（1–8）+ 生成按钮
- 题目区：每题左右分栏 `[3:2]`，**左作答右解析**；提交后展示三段式解析 + 错题「AI 深度解析」按需触发

**Tab3 课程 AI 助教：**
- 顶部上下文提示（当前课程/章节）→ 聊天历史（`st.chat_message`）→ 底部 `st.chat_input`

### 2.3 视觉与交互细节

| 组件 | 呈现形式 | 备注 |
| --- | --- | --- |
| 关键词 | 优先 `st.tags`，实测 1.45.1 不支持 → **自动回退 Markdown 反引号胶囊** | `hasattr` + `try/except` 双重兜底，渲染稳定 |
| 摘要/提示 | `st.info` / `st.caption` 语义色块 | 信息分层清晰 |
| 思维导图 | Mermaid `mindmap`（LLM 定制优先 → 离线规则兜底） | 有特殊字符清洗 `_sanitize_mermaid` |
| 题目解析 | `st.success`（为什么对）/ `st.warning`（错在哪）/ `st.info`（面试加分） | 颜色语义化，符合求职学习场景 |
| 答题进度 | 侧边栏 `st.progress` + 正确率百分比 | |
| 交互反馈 | `st.spinner`、`st.success/error/warning`、`st.toast` | 回发式交互，AI 调用为同步阻塞 |

---

## 3. 🚀 已实现的核心功能清单

### 3.1 课程与章节管理 ✅ 已完成
- 8 模块 42 门课静态大纲 + 模块级「重点模块」加权提示（`module_weight`）
- 模块 → 课程 → 章节**三级级联**选择（Tab1 + Tab2 两处独立实现）
- 大纲课程 ↔ 知识库 docx **自动绑定**（归一化 + 子串回退），侧边栏实时显示「知识库覆盖 x/42」
- docx **增量解析**（指纹缓存），新文件放入目录刷新即识别并提示「新增/更新课程」
- 未绑定课程优雅提示（引导放入对应 `课程名_原文/_导读.docx`）

### 3.2 媒体与字幕同步 ⚠️ 部分实现 / 待对接
- ✅ **时间戳分段文本**：原文按 `00:05`/`01:09:23` 时间戳解析为 `segments[{ts, text}]`，导读解析为 `sections[{ts, title, body}]`
- ❌ **未实现**：无任何音视频播放器、无字幕时间轴同步 UI、无音频文件接入；「媒体播放」目前只体现在"时间戳标注的纯文本"层面（章节速览、原文摘录），**是后续对接收音/视频课程的重要缺口**

### 3.3 AI 智能学习看板 ✅ 已完成（双模：AI 在线 + 离线兜底）
- **口语去噪清洗**：规则版（`kb.clean_text`，噪声词表 + 重复字压缩）与 AI 结构化萃取两套管线
- **核心关键词**：AI 萃取（3–8 个）→ 回退导读关键词
- **全文摘要**：AI `summary_points` → 回退导读摘要
- **定制思维导图**：按求职方向定制 Mermaid（LLM 优先）→ 回退离线规则版（关键词/章节/方向重点骨架）
- **实战/面试考点**：`interview_points`（AI 萃取，2–5 条）
- 全部结果带 **session 缓存**（`cleaned_cache` / `mindmap_cache`），避免重复调用

### 3.4 复习与评估系统 ✅ 已完成（强依赖 LLM，离线兜底）
- **AI 实战出题**：基于清洗干货 + 求职方向，出场景选型/异常调优/原理对比题；结构化 JSON（`correct_reason` / `wrong_reasons` / `interview_tips`）
- **出题范围**：全部课程 / 按模块（多选课程）/ 按章节（多选章节，AI 只提取该章节文本精准出题）
- **离线兜底**：内置题库（7 岗位 × 5 题）随机抽样，保证无 Key 可跑
- **提交判分**：完整性校验 → 统计正确率 → 错题入库
- **错题本**：侧边栏汇总 +「重刷错题」一键重练 + 去重
- **AI 错题深度解析**：仅做错且有 Key 时按需触发（`为什么选 A 不选 B` 三段式）
- **课程内测评**：Tab1 详情页内嵌「求职实战模拟测评」，独立作答状态防串课、防与 Tab2 互相干扰
- **学习统计**：累计答题数 / 正确率 / 错题数

### 3.5 其他
- **AI 课程助教**：基于当前课程/章节上下文问答，带最近 6 轮历史，切换课程自动清空 ✅
- **自动降级链路**：任一 AI 环节失败均有提示 + 离线回退，全站无 Key 可完整演示 ⭐ 设计亮点

---

## 4. 💡 当前版本的优缺点与改进空间

### 4.1 优势

1. **「AI 优先 + 离线兜底」双模架构**：所有 AI 能力都有规则版降级，未配置 Key 也能完整体验全流程，对演示与教学场景极友好。
2. **数据管线完整**：docx → 时间戳分段 → 口语去噪 → 结构化萃取 → 定向出题，从"原始逐字稿"到"求职真题"链路闭环。
3. **求职方向贯穿全站**：思维导图定制、实战出题、错题解析、AI 助教均被 `career_direction` 驱动，产品立意统一。
4. **结构化解析设计优秀**：`correct_reason / wrong_reasons / interview_tips` 三段式直击求职痛点，比单一 explain 信息密度高得多。
5. **渲染稳定性兜底**：`st.tags` 回退、Mermaid 渲染回退、JSON 解析多格式兼容，抗环境差异能力强。
6. **状态隔离意识强**：`cans_*` vs `ans_*`、`current_quiz_course` 防串课、`quiz_direction` 方向切换联动，交互边界清晰。
7. **零重型依赖**：docx 用 zipfile+正则手写解析，全项目仅 2 个依赖，部署成本极低。

### 4.2 不足与痛点

**架构层面：**
1. **单文件臃肿**：`app.py` 1549 行同时承担 UI、LLM、数据、题库、业务，模块边界模糊，后续扩展（如新增学习模式）成本高，建议按 `llm/`、`data/`、`views/` 拆分。
2. **无用户级持久化**：错题本/统计仅存 `st.session_state`，刷新即丢失；无多用户隔离。建议引入 SQLite（本地）或轻量后端。
3. **AI 调用无容错参数**：`call_llm` 未设置 `timeout`、无重试/指数退避，网络抖动时直接抛异常（虽有 catch 但体验一般）。
4. **媒体能力缺失**：无音视频播放器与字幕时间轴同步，与"音视频课程"原始定位不匹配，是最重要的功能缺口。

**UI/UX 层面：**
5. **Tab1 详情看板过长**：关键词→摘要→导图→干货→测评→章节速览垂直堆叠，信息密度高但需大量滚动；思维导图与测评可考虑折叠或挪入独立弹层/子页签。
6. **离线题库质量偏低**：7 个岗位题目答案**全部为选项 A**（`answer: 0`），干扰项趋于"明显错误"，用户可秒猜正确项；建议重写题库并随机化正确项位置。
7. **AI 调用为同步阻塞**：无流式输出（`stream=True`），出题/解析期间只能等待 spinner，长文本时体验僵硬。
8. **移动端/无障碍弱**：`wide` 布局 + 双栏在窄屏下挤压严重；依赖 emoji 表意，读屏支持一般。

**工程/数据层面：**
9. **知识库覆盖极低**：当前仅 1 门课（开学典礼）入库，42 门课中 41 门处于"未绑定"状态，核心功能缺少真实数据验证。
10. **解析规则脆弱**：docx 结构靠「关键词/全文摘要/章节速览」字面行触发，文档格式微调即失效，缺格式校验与告警。
11. **上下文截断**：萃取/出题上下文 `max_chars` 截断，长课程原文可能丢失尾部关键信息；无智能摘要优先策略。
12. **测试薄弱**：仅临时冒烟脚本（无 pytest），判分、缓存、降级链路的边界用例缺失。

### 4.3 改进路线建议（按优先级）

| 优先级 | 改进项 | 建议 |
| --- | --- | --- |
| P0 | 补齐媒体播放与时间戳联动 | 接入音频/视频文件 + 播放器（`st.audio/video` 或前端组件），点击章节时间戳跳转播放位置 |
| P0 | 重写离线题库 | 选项随机化、干扰项更真实、覆盖各模块考点 |
| P1 | 引入 SQLite 持久化 | 错题本/统计/对话历史落盘，支持会话间保留 |
| P1 | 按模块拆分 app.py | llm 层 / 数据层 / 渲染层解耦 |
| P1 | AI 调用容错 | 加 timeout、重试、流式输出、按内容长度分段出题 |
| P2 | UI 减负 | 详情看板改为「概览 + 子页签/折叠」层级 |
| P2 | 补充课程数据 | 批量导入其余 41 门课 docx，验证全链路 |
| P2 | 建立测试体系 | pytest + AppTest 覆盖核心链路与降级路径 |

---

## 附：关键事实速查

- 代码规模：`app.py` 1549 行 + `kb.py` 544 行，共约 2100 行 Python
- 依赖：仅 `streamlit>=1.30`、`openai>=1.30`（实测 streamlit 1.45.1）
- 数据规模：8 模块 / 42 门课 / 4 求职方向 / 7 岗位离线题库（35 题）/ 知识库 1 门课（2 个 docx）
- 运行方式：`streamlit run app.py` → `http://localhost:8501`
