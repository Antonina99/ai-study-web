# 开发规范与动态进度追踪 (DEVELOPMENT.md)

> **AI 助手须知**：
> 1. 本文档为**动态文档**。在每次完成重大功能、修复 Bug 或本次对话额度即将用完时，**必须更新【Section 3: 当前开发进度】**。
> 2. 编写任何代码时，必须严格遵守【Section 1: 编码与安全规范】。

---

## 1. 编码与安全规范 (Coding & Safety Rules)

### 1.1 命名与风格
* **Python（本项目唯一语言）**：
  * 文件名：模块统一用 `snake_case`（如 `tab1_course.py`、`llm.py`）。
  * 变量与函数：`snake_case`；模块级常量：`UPPER_SNAKE_CASE`（如 `CAREER_DIRECTIONS`、`DEFAULT_MODEL`）。
  * 遵从 PEP 8：函数/变量命名清晰达意，禁止拼音缩写；类用 `PascalCase`。
* **代码文档**：
  * 所有模块必须编写模块级 Docstring（`"""..."""`）说明模块职责；每个函数编写简要 Docstring 说明参数与返回。
  * 新增/修改函数时尽量补充类型提示（Type Hints），存量代码可不回填。

### 1.2 代码质量与架构约束
* **函数复杂度**：单个函数原则上不超过 50 行，保持单一职责；过长的逻辑应拆分为内部函数或下沉到 `core/` 层。
* **严禁重复造轮子**：新增逻辑前，优先检索 `core/`（`llm.py` / `data.py` / `kb.py`）与 `views/` 中是否已有同类函数（如去噪、归一化、洗牌、JSON 解析、Prompt 构造）。
* **分层约束（强制）**：
  * `views/` 与 `app.py` 禁止直接创建 `openai` client 或发起 API 请求——LLM 调用只能走 `core/llm.py`。
  * `views/` 禁止写复杂业务逻辑（出题规则、docx 解析、结构化解析等一律调 `core/` 层）。
  * 新增会话状态必须在 `app.py::init_session_state()` 中声明默认值（运行期键如 `ans_*` / `cans_*` 除外）。
* **防御性编程**：
  * 所有 LLM / 外部调用必须 `try-except` 包裹，并按「AI 优先 + 离线规则兜底」双模降级，禁止裸异常导致页面崩溃。
  * 对空值（`None` / 空列表 / 空文本）显式校验后再使用，避免运行时崩溃（参考现有 `generate_practical_quiz` 的空值短路）。

### 1.3 安全与敏感数据
* **严禁硬编码**：API Key、Token 等敏感信息**严禁写死在代码中**。本项目 API Key 由用户在侧边栏输入、存 `st.session_state.api_key`（仅内存、不入库、不落盘）；如后续需要环境变量方式（`.env` / `OPENAI_API_KEY`）统一配置，必须先与用户确认。
* **日志与提示规范**：禁止在 `print` / `st.write` / `st.error` 等任何输出中打印完整 API Key 或敏感用户数据；LLM 错误展示一律经 `core/llm.py::humanize_error()` 中文化后再上屏。

---

## 2. 协作与 Handover 工作流 (AI Transition Workflow)

当当前 AI 额度即将耗尽、需要切换到另一个 AI（如从 CodeBuddy 切换到 Trae）时，按以下两步操作：

1. **当前 AI 收尾指令**：
   > *"请根据我们刚才的改动，更新 `DEVELOPMENT.md` 中的【3.1 已完成】、【3.2 进行中】和【3.3 遗留卡点/上下文】，并给出下一步建议。"*
2. **接手 AI 启动指令**：
   > *"你现在接手这个项目。请先读取 `PROJECT_SPEC.md` 和 `DEVELOPMENT.md`，了解架构规范与当前进度，然后继续完成 `DEVELOPMENT.md` 中【进行中】的任务。"*

---

## 3. 当前开发进度 (Progress Tracking)

> **每次改动代码后，请 AI 自动在此处打勾或更新状态**。

### 3.1 已完成功能 (Completed)
* [x] **[2026-09-08]** 岗位课程路径复核：以完整课程名配置四级学习定位（基础必学、岗位核心、专项选学、求职准备）、推荐理由与学习范围，替换标题子串匹配。补齐应用开发的检索与部署课程；视觉、昇腾改为专项；产品/解决方案架构及编程/办公提效支持独立路径选择（仅影响课程路径）。提示未确认的课程覆盖缺口，保持原生折叠目录。22 项回归测试、AppTest 启动及侧重点切换验证通过。
* [x] **[2026-08-30]** 项目分层重构：`core/`（llm / data / kb）+ `views/`（tab1~tab4）+ `app.py` 入口，职责边界明确
* [x] **[2026-08-30]** docx 知识库管线：zipfile+正则解析、时间戳分段、口语去噪、课程名归一化、增量指纹缓存（`_knowledge_cache.json`，运行期自动发现新 docx）
* [x] **[2026-08-30]** 业务数据层：5 大求职方向、5 大核心模块课程大纲、3 门课程 docx 自动绑定索引、新增课程 AI 智能分类（`EXT_MODULE_NO` 兜底）
* [x] **[2026-08-30]** AI 能力全链路（**AI 优先 + 离线兜底**双模）：结构化萃取、求职方向定制思维导图（Mermaid）、AI 实战出题、错题深度解析、AI 助教流式问答（`st.write_stream`）
* [x] **[2026-08-30]** 离线题库（5 方向 × 5 题）+ 选项洗牌（`shuffle_question`）+ 出题失败逐级降级（AI → 离线题库）
* [x] **[2026-08-30]** 四个 Tab UI：课程大纲与导读（三级级联 + 课程内测评）、智能自测与刷题、课程 AI 助教、错题复习（JSON 导出 + 重刷错题）+ 学习进度看板
* [x] **[2026-08-30]** LLM 层容错：超时、指数退避重试、JSON 多格式解析、错误中文化（`humanize_error`）
* [x] **[2026-08-30]** 文档基线：`PROJECT_SPEC.md`（架构规范）、`DEPLOY.md`（部署指南）、`CODEBUDDY.md`（协作规则）
* [x] **[2026-09-06]** 明确后续产品方向并重排待办：以音视频转出的文字构建学习工具，取消播放器方向。本轮最终交付为开发计划更新，试写的业务代码已撤回，接手前已有改动保留。
* [x] **[2026-09-06]** P0-1 转写文字解析修复：兼容「发言人 / 说话人 / 讲师 + 可选名称或编号 + 时间戳」及全角冒号，完整保留 `HH:MM:SS`；为知识库缓存增加解析版本，规则升级后自动重建。现有 3 门课从源 docx 分别解析出 300 / 311 / 321 个原文段落，隔离缓存回归与 Streamlit 启动验证通过。
* [x] **[2026-09-06]** P0-2 原文单文件学习路径：无导读课程可从转写原文生成统一章节目录、阅读清洗正文并参与章节选择；无时间戳时保留原始段落且不伪造时间，空文档会入库并显示明确状态。Tab1、Tab2 与学习进度统一复用同一章节数据，缓存版本升级至 3。
* [x] **[2026-09-06]** P0-3 章节上下文范围修复：导读章节按“本章起点（含）到下一章起点（不含）”提取完整原文，最后一章覆盖到原文结束；时间戳统一换算为秒，准确支持 `HH:MM:SS`。无时间戳课程按 `original-N` 精确定位；AI 助教、AI 出题与离线知识库出题复用同一取文函数，章节出题不会误用整课清洗缓存。
* [x] **[2026-09-06]** 工作台 UI 改造：保留四个业务入口和原有数据逻辑，新增统一浅灰背景、白色卡片、细边框、紧凑排版及响应式样式；应用级导航、状态标签、指标卡和学习进度接入 `streamlit-shadcn-ui`。课程学习改为目录 + 内容双栏工作台，AI 助教改为上下文 + 对话 + 快捷提问三栏，自测控制区、题目和错题页统一卡片风格。
* [x] **[2026-09-06]** P1-1 长课程整理与内容定位：整课原文按章节边界打包为有界文本块，AI 逐块萃取后均衡聚合关键词、摘要、考点和清洗正文；整课与单章 LLM 上下文均衡覆盖各章节/片段。整理缓存键包含知识库课程名、源内容指纹和模型；课程页新增原文关键词检索、分页浏览与稳定片段标识，章节页可直接展开本章完整原文。
* [x] **[2026-09-06]** P1-2 问答与题目原文依据：助教、课程测评、智能自测和错题深度解析统一携带课程名、章节、真实 `original-N` 标识与原文；引用先从本地课程/章节检索，再对白名单校验，模型编造的片段标识会被移除。助教回答和错题解析区分课程原文与模型补充知识，无直接依据时明确提示。
* [x] **[2026-09-06]** P1-3 课程内测评离线降级：课程页测评改为 AI 课程题 → 当前课程文字题 → 通用内置题三级链路，无 Key、AI 调用失败或空返回时仍可生成题目；题目明确显示来源类型。课程/章节共同组成测评范围键，切换范围不沿用旧题，作答、判分、错题记录继续使用课程页独立状态。
* [x] **[2026-09-06]** P1-4 文字学习链路回归验证：新增标准库 `unittest` 套件，覆盖说话人前缀、小时级时间戳、无时间戳稳定段落、空输入、原文单文件入库、缓存命中与变化重建、章节边界、答案映射及 AI 空返回降级；测试使用临时 `.docx`、临时缓存和模拟模型结果，不修改课程源文件或调用真实 API。
* [x] **[2026-09-06]** P1-5 第一批真实课程验收：完成 44 门大纲课程盘点，现有 3 门课程均通过发现、绑定、阅读、章节、整课分块、问答依据和离线出题检查；新增真实资料验收测试与 `docs/course_content_status.md`。同时修复 `build_qa_request()` 返回语句错位导致实际助教提问返回 `None` 的遗留卡点，并加入回归用例。
* [x] **[2026-09-06]** P2-1 文字入库与部署文档校正：重写 `DEPLOY.md`，补全原文单文件、可选导读、DOCX 示例、时间戳、缓存刷新、排错、本地/云端启动及资料授权说明；删除不存在的分享模块、SQLite 和课程级分享流程。依据已安装依赖元数据将最低 Python 版本从 3.9 校正为 3.10，并同步 `PROJECT_SPEC.md` 与入口说明。
* [x] **[2026-09-06]** P2-2 临时诊断脚本整理：审查根目录 7 个 `_diag_*.py`，将课程名绑定、AI 分类异常兜底和选项前缀清洗等有效能力迁移为 5 个正式回归测试，删除重复的人工调试脚本；全套 15 项测试通过。
* [x] **[2026-09-07]** P1-6 课程干货清洗与去重：新增句子级非知识内容过滤、口语压缩、相邻短语去重和整课近似句去重；离线导读与 AI 分块结果统一经过后处理，关键词排除直播运营与课程事务标签，AI 萃取提示词明确删除寒暄、闲聊、宣传和结束语。课程原始转写与 `original-N` 引用保持不变。
* [x] **[2026-09-07]** P2-3 可折叠知识图谱 UI：知识图谱改为原生交互树，上半区按模块折叠展示完整课程路径，并随所选求职方向明确标记岗位重点课程、当前课程和已有文字；下半区按核心主题、课程脉络、实践考点与求职关联折叠展示当前课程知识。移除 Mermaid、图例和源码入口。

### 3.2 进行中 / 当前上下文 (In Progress)
* **进行中：P1-5 后续真实课程批次**。第一批现有 3 门已验收；其余 41 门没有对应 DOCX，保持待补资料状态。
* **目标流程**：音视频在工具外转成文字 → 转写文字入库 → 课程导读与章节整理 → 结合原文问答 / 刷题 → 错题复习。
* **固定回归命令**：在项目根目录运行 `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，无需 API Key。
* **当前计划内可独立执行的代码与文档任务已完成**：P1-6 与 P2-3 已完成；P1-5 收到新的转写 DOCX 后，按模块 1 优先继续逐门验收，在资料到位前不编造课程内容。
* **本轮边界**：继续使用 Streamlit + Python 标准库 + openai SDK；当前输入仍为 docx；学习进度与错题本仍保存在 Session State；`PROJECT_SPEC.md` 的技术栈与核心架构不变。

### 3.3 待办事项 (TODO List)

按 **P0-1 → P0-2 → P0-3 → P1 → P2** 执行；每项完成后记录实现范围及验证结果。

#### 第一阶段 · P0：让转写文字可靠入库并可学习

* [x] **P0-1 修复转写原文与章节解析**：`core/kb.py` 已兼容现有文档中的「发言人   00:01」时间戳行及常见说话人变体，保留完整 `HH:MM:SS` 章节时间戳，并让解析规则升级后自动重建旧缓存。验收完成：现有 3 门课原文均非空；普通 / 说话人前缀 / 全角冒号 / 小时级时间戳回归通过；旧缓存触发重建，第二次加载命中缓存。
* [x] **P0-2 补齐“只有转写原文，没有导读”的学习路径**：新增统一章节数据组装；有时间戳时按转写时间段展示，无时间戳时逐段保留并使用稳定段落编号，不伪造时间。离线干货回退到清洗后的原文；空文档仍会入库并提示无可阅读文字。验收完成：原文单文件 / 无时间戳 / 混合时间戳 / 空文档 / 缓存重建均通过隔离回归，Streamlit 应用启动通过。
* [x] **P0-3 修正章节上下文范围**：`core/kb.py::section_original_segments()` 统一按章节起止区间提取原文，最后一章覆盖到原文结束；`timestamp_seconds()` 精确处理分钟级与小时级时间戳；无时间戳课程按 `original-N` 定位。AI 助教、AI 出题与离线出题均复用该范围，选中章节时跳过整课清洗缓存。验收完成：多分钟章节、末章、小时边界、无时间戳段落、旧 `section_ts` 兼容路径、问答 Prompt、AI/离线出题及 Streamlit 启动验证通过。

#### 第二阶段 · P1：提高导读、问答与刷题的可用性

* [x] **P1-1 改进长课程的整理与内容定位**：新增章节单元与文本块组装，超长章节连续拆分，AI 分块萃取后以轮询方式聚合各块资产；整课与单章上下文按单元均衡采样，避免只保留开头。缓存以课程名 + 源内容 SHA-1 指纹 + 模型隔离，已完成文本块可复用，部分失败只重试缺失块。课程页新增“原文查找”，支持多关键词同时匹配、分页浏览、时间戳与 `original-N` 定位，并可从章节页展开本章原文。验收完成：合成长课程头/中/尾覆盖、章节隔离、检索、缓存命中与跨课程隔离通过；现有 3 门课完整打包为 4 / 8 / 9 块，编译及 Streamlit AppTest 通过。
* [x] **P1-2 增强问答与题目解析的原文依据**：助教按问题从当前课程/章节检索真实转写片段，Prompt 只允许使用候选 `original-N`，回答后再次移除白名单外引用；回答固定区分“课程原文结论”与“模型补充知识”，页面以折叠面板展示本地重新读取的课程名、章节、时间戳、片段标识和完整文字。AI 课程测评、智能自测及错题深度解析复用同一引用结构，题目 JSON 引用经过白名单验证；无依据或内置通用题明确标注。验收完成：合成课程检索、章节/时间戳映射、虚构 `original-999` 拒绝、Prompt 分区、题目 JSON 解析及引用回填通过，编译和 Streamlit AppTest 通过。
* [x] **P1-3 补齐课程内测评的离线降级**：课程页“生成本课程测评”在离线模式也可用，生成顺序为 AI 课程原文题 → 当前课程文字题（区分原文/导读）→ 通用内置题，不足题数自动补齐；每题和生成结果均显示来源，无原文引用时明确说明。修复课程测评只显示 A/B/C/D 而不显示选项文字的遗留问题。测评范围键包含课程与章节，切换范围会清空旧题；`current_quiz/current_submitted/cans_*` 与 Tab2 的 `quiz/submitted/ans_*` 保持隔离。验收完成：无 Key、模拟 AI 空返回、空课程、章节范围、题数补齐、答案索引、来源标记、完整选项显示和未答拦截验证通过，编译及 Streamlit AppTest 通过。
* [x] **P1-4 建立文字学习链路回归验证**：新增 `tests/test_text_learning.py`，使用标准库 `unittest` 覆盖说话人前缀、`HH:MM:SS`、无时间戳及 `original-N`、缺导读、空输入、临时 docx 入库、缓存首次写入/二次命中/内容变化重建、首章/中段/末章范围、选项洗牌后的答案映射、AI 空返回时课程题降级和空课程通用题降级；随后加入助教请求返回值、真实课程端到端验收、课程索引、干货清洗去重、可折叠知识分组和五岗位重点课程映射用例。固定命令 `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`；全套 21 项测试通过，无真实 API 请求，临时测试不读写现有课程源文件和缓存。
* [ ] **P1-5 分批补充真实课程文字（第一批已完成）**：现有 3 门课已逐门通过发现、绑定、阅读、问答和离线出题验收：开学典礼 300 片段/34 章/4 块；提示工程到 RAG 311 片段/80 章/8 块；Agent 321 片段/80 章/9 块。44 门完整盘点见 `docs/course_content_status.md`；当前已入库 3、待补原文 41、缺导读 0、解析失败 0。后续按用户提供的转写 DOCX 分批继续，缺资料课程保持待补状态，不编造原文。
* [x] **P1-6 课程干货清洗与去重**：`core/kb.py::clean_text()` 现按句过滤寒暄、直播互动、课程事务、闲聊和结束语，压缩重复口头表达；`clean_course_text()` 在整课范围去除相同及高度相似句。离线导读、原文兜底、AI 单块结果和跨块合并统一后处理，AI 整理缓存版本升级至 2；关键词排除直播/课表/资源领取等运营标签。验收完成：“拜拜、下周二见”、闲聊、重复句删除，Transformer 数字参数保留，真实课程回归性能保持在秒级。

#### 第三阶段 · P2：完善维护与交付文档

* [x] **P2-1 更新文字入库与部署说明**：`DEPLOY.md` 已按当前文字学习产品重写，说明 Python 3.10+ 环境、本地/服务器启动、`课程名_原文.docx` 必需与 `_导读.docx` 可选路径、无时间戳段落、`MM:SS` / `HH:MM:SS` / 说话人前缀、导读字面标记、增量缓存、排错、Community Cloud、自管服务器与内网穿透。已删除不存在的 `core/share.py`、`learning_tracker.db`、课程级分享链接和分享侧边栏步骤，并明确 Session State 与 API Key 的实际生命周期。验收完成：文档命令与依赖、入口、目录和页面一致；临时 DOCX 入库及应用启动由当前 15 项回归测试和 Streamlit AppTest 复现。
* [x] **P2-2 整理临时诊断脚本**：已审查根目录 7 个 `_diag_*.py`，确认 5 个课程 AI 分类脚本为同一问题的阶段性调试，另外 2 个分别验证名称匹配与选项清洗。有效场景已迁移到 `tests/test_course_index.py` 的 5 个隔离用例，原脚本全部删除；仓库内无代码或文档依赖这些脚本，未删除课程资料，完整 15 项回归通过。
* [x] **P2-3 改造可折叠知识图谱 UI**：使用 Streamlit 原生 Expander 构建两层可交互知识树。岗位课程路径包含 5 个模块和完整课程清单，模块折叠标题显示重点课数量；课程节点按五套显式岗位映射显示“岗位重点”，并显示“当前课程”“已有文字”状态。当前课程知识结构按核心主题、课程脉络、实践考点和求职关联分别折叠。默认展开当前课程所在模块及第一个知识分支；移除 Mermaid 图、图例、结构代码和源码入口。

### 3.4 待确认 / 暂缓事项

* **学习进度与错题持久化**：从原 P0 移出当前主线。确有跨会话保存需求时，再比较 JSON 导入导出与 SQLite，确认保存内容、部署位置及方案后实施；当前不得擅自落盘。
* **TXT / 粘贴文字等输入扩展**：先把现有 docx 路径做好；根据实际转写工具的输出格式决定是否增加。涉及知识库唯一数据源约定的变更时，先确认范围并同步规范。
* **新增依赖与基础设施**：当前不预设 pytest、向量数据库、独立后端或转写服务。只有现有标准库与既有依赖无法满足具体需求时，再评估引入。

### 3.5 已取消方向

* **[2026-09-06 用户明确取消] 音视频播放与章节时间戳联动**：不再开发播放器、媒体上传或字幕同步。时间戳仅作为文字分段和原文定位信息；音视频转文字由工具外完成。

---

## 4. 遗留卡点与关键上下文 (Context & Edge Cases)

> 本区域记录开发过程中发现的折衷方案、临时调整或未解决的问题，防止接手 AI 踩坑。

* **[产品方向 · 2026-09-06]**：用户明确取消音视频播放，使用音视频转出的文字构建学习工具。当前继续通过 `课程名_原文.docx / 课程名_导读.docx` 接入转写文字；播放器、媒体上传与字幕同步均不再是待办，不要按旧计划恢复开发。

* **[解析问题 · 2026-09-06 已解决]**：第 2、3 门课的「发言人   00:01」前缀和导读小时级时间戳均已修复；同时兼容「说话人 / 讲师」、可选名称或编号及全角冒号。验证直接从源 docx 解析，不依赖运行缓存。
* **[旧缓存误判 · 2026-09-06 已解决]**：缓存现含 `cache_version`；版本缺失或过期时 `has_file_changes()` 会要求刷新，`build_kb()` 会忽略旧指纹并从源 docx 重建，之后恢复增量缓存。
* **[原文单文件路径缺口 · 2026-09-06 已解决]**：`course_sections()` 在缺导读时从转写原文生成章节；`_rule_cleaned_doc()` 在离线模式回退原文。Tab1 目录 / 详情、Tab2 章节选择及进度统计均复用该数据。无时间戳段落使用 `original-N` 标识，不冒充时间戳。
* **[章节上下文截断 · 2026-09-06 已解决]**：已移除 `ts[:5]` 的分钟前缀匹配。章节上下文、AI/离线出题统一调用 `section_original_segments()`；区间上界取下一导读章节的完整时间戳，最后一章无上界。选中章节时不再回退到整课清洗缓存，避免问答或题目混入邻章内容。
* **[工作台 UI · 2026-09-06]**：全局视觉集中在 `assets/styles.css`，可复用组件集中在 `views/workspace_ui.py`。新增依赖 `streamlit-shadcn-ui>=1.0,<2`，因此 Streamlit 最低版本同步提升至 1.60；当前开发环境使用 Streamlit 1.63.0、streamlit-shadcn-ui 1.3.0。
* **[长课程固定裁剪 · 2026-09-06 已解决]**：`course_context_chunks()` 按章节边界覆盖整课并拆分超长章节，`get_cleaned()` 逐块整理后组装。整课问答/出题使用均衡后的整理上下文，单章问答在章节内按转写片段均衡取样。整理缓存不再仅使用大纲课程 ID，源文变化或模型变化会生成新缓存键。
* **[课程干货噪声与重复 · 2026-09-07 已解决]**：原离线路径直接拼接导读、AI 分块合并缺少跨块去重，现统一经过句子过滤和整课去重。过滤仅影响学习视图的派生文本，不改写知识库中的原始转写；原文查找和引用验证仍读取原始 `original-N`。
* **[知识图谱串课与源码暴露 · 2026-09-07 已解决]**：旧 `_career_mindmap()` 忽略当前课程且缓存只按方向区分。当前改为即时组装的可折叠岗位课程路径与课程知识树；岗位重点来自五套明确映射，页面不再依赖 Mermaid，也不展示图例 HTML、结构说明或源码。

* **[部署文档不一致 · 2026-09-06 已解决]**：`DEPLOY.md` 已移除不存在的 `core/share.py`、`learning_tracker.db` 和“对外分享”入口，部署步骤现与实际应用一致。
* **[旧文档过时]**：`项目现状汇总报告.md` 是重构前的分析（描述 app.py 1549 行单文件架构），**已过时**，勿作为当前代码依据；当前实际架构见 `PROJECT_SPEC.md`。
* **[解析规则脆弱]**：`core/kb.py` 的导读解析依赖 docx 中「关键词 / 全文摘要 / 章节速览」字面标记行，文档格式微调即失效。新增课程 docx 必须遵循命名规则「课程名_原文.docx / 课程名_导读.docx」，否则不会入库。
* **[会话级数据设计]**：学习进度（`completed_chapters`）与错题本（`error_notebook`）仅存 Session State，刷新页面即清空——这是当前有意设计（无持久化库），「Tab 4 错题复习」的 JSON 导出是唯一备份途径。未与用户确认前不要擅自改为落盘。
* **[API Key 无环境变量]**：API Key 由访客在页面侧边栏输入、仅存会话内存，无 `.env`、无服务端统一配置。云端部署时每个访客需自行填 Key（见 DEPLOY.md 通用注意事项第 6 条）。
* **[出题降级是预期行为]**：AI 实战出题失败（无 Key / 解析失败 / 网络异常）会静默降级为离线题库并给出提示，属预期设计；排查出题异常时应先确认 Key 有效性与 LLM 返回格式（`core/llm.py::parse_question_json`）。
* **[AI 分类去重机制]**：`refresh_kb()` 对「新增课程」的 AI 分类按批次签名去重（`_kb_classify_sig`），同一批课程只调一次 LLM，避免 Streamlit 每次 rerun 重复调用——改动该逻辑时注意保留此防抖。
* **[状态隔离]**：Tab1 课程内测评（`cans_*` / `current_quiz`）与 Tab2 自测（`ans_*` / `quiz`）使用独立状态键防串课；新增答题类交互时必须保持这种隔离模式。
